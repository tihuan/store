Hello World!

<% @accounts.each_with_index do |a, i| %>
  <p>
    Member ID: <%= i %>
    First Name: <%= a.fname %>
    Last Name: <%= a.lname %>
  </p>
<% end %>

<h3><%= link_to('Create Account', new_account_path) %></h3>
<h3><%= link_to('Create Appointment', new_appointment_path) %></h3>
